package main

func txTestsWithDocDb(conxStr string) {
	// err := obinary.OpenDatabase(dbc, dbDocumentName, constants.DocumentDB, "admin", "admin")
	// Ok(err)
	// defer obinary.CloseDatabase(dbc)

	// wilson := oschema.NewDocument("Cat")
	// wilson.Field("name", "WilsonX")
	// wilson.Field("age", 10)
	// wilson.Field("caretaker", "Tricia")
	// // wilson.save();

	// malcolm := oschema.NewDocument("Cat")
	// malcolm.Field("name", "MalcolmX")
	// malcolm.Field("age", 37)

}
